namespace DataVanger.Infrastructure.Shell;

/// <summary>
/// Placeholder marker for the DataVanger.Infrastructure project shell
/// introduced in Phase 2 Step 01 (Project Split). The Infrastructure project
/// is intentionally empty at this phase — Windows-specific adapters
/// (filesystem, registry, ETW, DPAPI, IPC, signing) continue to live inside
/// the DataVanger main assembly's Infrastructure/ and Runtime/ folders and
/// will be migrated incrementally by later, focused phases.
/// </summary>
internal static class InfrastructureProjectMarker
{
    public const string Phase = "Phase 2 / Step 01 / Infrastructure shell";
}
